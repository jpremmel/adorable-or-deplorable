import React from 'react';
import { Switch, Route } from 'react-router-dom';

function Header() {
    return(
        <div>
            <h2>Adorable or Deplorable</h2>
        </div>
    );
}

export default Header;